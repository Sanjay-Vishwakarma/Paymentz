import com.logicboxes.util.Util;
import com.directi.pg.Functions;

import java.io.File;
import java.io.IOException;
import java.io.FileWriter;
import java.util.Calendar;
import java.util.Hashtable;
import java.util.TimeZone;
import java.text.SimpleDateFormat;
import java.math.BigDecimal;

/**
 * Created by IntelliJ IDEA.
 * User: admin
 * Date: May 2, 2010
 * Time: 11:48:31 PM
 * To change this template use File | Settings | File Templates.
 */
public class Test
{
    public static void main(String[] args) throws Exception
    {
        /*Calendar cal = Calendar.getInstance();
        SimpleDateFormat dateFormat = new SimpleDateFormat("yyMMdd");
        java.util.Date currentDate = cal.getTime();
        System.out.println("date ="+ dateFormat.format(currentDate));

        cal.set(2011,7,31);
        System.out.println(" time "+cal.getTime());
        cal.add(Calendar.DAY_OF_YEAR, -365);
        System.out.println(" time "+cal.getTime());
       // cal.add(Calendar.MONTH,+1);
       // System.out.println(" time "+cal.getTime());
       // cal.set(Calendar.DAY_OF_MONTH,1);
        //System.out.println(" time "+cal.getTime());

        cal.set(cal.get(Calendar.YEAR),cal.get(Calendar.MONTH)+1,1,0,0,0);
        System.out.println(" time "+cal.getTime());*/

        /*String ccpan = "4111111111111881";

        String newcc = ccpan.substring(ccpan.length() - 4, ccpan.length());
        System.out.println("new c "+newcc + " and "+ccpan.substring(ccpan.length() - 4));*/




        File f1 = new File("C:/tomcat/common/chargeback" + "/" + "CYB111017V.TXT");
                    f1.renameTo(new File("C:/tomcat/common/chargeback" + "/temp/" + f1.getName()));
        

    }

    private static String getRefNo(String line)
    {
        String[] fields = line.split("\\|");
        String ARN = fields[8];
        String referenceNo = null;
        System.out.println("ARN ="+ARN);
        if (ARN != null)
        {
            String year = String.valueOf(Calendar.getInstance().get(Calendar.YEAR));
            String singleDigitYear = year.substring(year.length() - 1);

            referenceNo = singleDigitYear + ARN.substring(11, ARN.length() - 1);

        }

        return referenceNo;
    }
}
